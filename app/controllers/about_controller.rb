class AboutController < ApplicationController
    def index
        # doesn't need to do anything because inheriting from ApplicationController
        # when this is processed it will say if this does any rendering then use that, if not use rails default render 
    end
end